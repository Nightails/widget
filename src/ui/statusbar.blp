using Gtk 4.0;
using Astal 4.0;

template $StatusBar: Astal.Window {
    CenterBox centerbox {
        end-widget: Box {
            spacing: 4;
            Label {
                label: bind template.calendar;
            }
            Label {
                label: bind template.clock;
            }
        };
    }
}
